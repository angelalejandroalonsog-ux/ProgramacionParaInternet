NOTAS CLASE Programación para internet. 20/08/26	

	GIT STUDENT'S PACK.

	Dir para listar lo que tenemos en el directorio 

	cd es de "Change Directory"
	
	cd .. Para salir del directorio actual

	cd ..\.. para ir atrás dos directorios

	en Linux: Usamos 'pwd' para ver en qué parte del directorio nos encontramos.
		En Linux usamos lr también como dir en windows
	git init: hace unos archivos de seguimiento para git	

	git status: nos muestra el estado de los commit y los añadidos

	git add . añade todo lo que se encuentra en nuestro actual directorio

	git add example.html añadiría un archivo en especifico.

	git commit: no entendí bien, pero al hacer status, ya chanchó

	git log: nos muestra nuestro historial de commit's

	git rm: para remover algun elemento del stage.

	git config: user.name/ user.email /init.defaultBranch

	git commit -m: con esto cambiamos el nombre del commit o snapshot actual.

	SecureShell: información encriptada, comunicación segura. Las llaves se crean en una carpeta oculta en la carpeta del usuario.

	con: ssh-keygen -t ed25519 -C "email@example.com" Creamos una clave única, donde nos genera una llave pública para conectarnos remotamente,
	y la clave privada, con la cual nos verificamos.

----------------------------------------------------------------------------------------------------------------------------------------------------

	25/08/26

	el git pull pude hacerlo bien hasta que añadí el enlace ssh que otorga github (git pull git@github.com:angelalejandroalonsog-ux/ProgramacionParaInternet.git)
	
	git branch -m master main es la forma correcta de cambiar el nombre del branch.

	git checkout -b nueva-rama //crea una nueva rama y muéveme a esa rama

	mejor ponerle el nombre de lo que estamos trabajando a las ramas, no una descripción cualquiera, pues se integran o se eliminan, etc.

	desde una rama alternativa podemos hacer git checkout -b para movernos a una rama que deseemos.
	a partir de hacemos un git fetch para actualizar 

	podemos usar fast forward para integrar dos ramas. Pero puede terminar siendo demasiado grande el historial de los commit, conviene hacerlo con programas más pequeños.
	En proyectos grandes es mejor usar No Fast forward, el cual usa un commit de integración, uniendo todos los cambios de una rama en un sólo commit.
	squash //no alcancé a escuchar.

	Podemos usar algún entorno gráfico para hacer este tipo de funciones. //Fork, GitKraken, Git Desktop.

	en VSCode, con ctrl+p podemos hacer una búsqueda de archivos en proyectos grande

	en VSCode podemos ver el origen de los cambios en nuestro branch, esto al momento de haber un conflicto es más fácil de resolver en vscode

	para hacer commit's es mejor hacerlo desde un entorno gráfico //creo haber escuchado.

	GIT CHEAT SHEET'S

	la primera vez que vamos a hacer un git push, se hace : git push -u origin nombre-de-la-rama
	el resto de veces podemos simplemente hacer git push

	los commit's deben reflejar los cambios hechos, por costumbre uno debe poner mensajes de commit adecuados, no como "ultimo cambio"
-------------------------------------------------------------------------------------------------------------------------------------------------
	01/09/26

	Para cargar plantillas podemos sacarlas de Bootstrap y Tailwind (primera clase donde vemos CSS)
	clases para cambiar cosas //creo que toca verse algún tutorial

--------------------------------------------------------------------------------------------------------------------------------------------------
	03 09 26
	al parecer vimos los formularios.
-------------------------------------------------------------------------------------------------------------------------------------------------
	08 09 26

	los comentarios hechos en php no deberían de ser visible como en html
	las variables deben empezar por una letra o un guion bajo, no puede empezar por un número.
	las variables que son "variables" generalmente van en minúsculas y las variables "constantes" se escriben en mayúsculas.
	
	PhP nació como una versión para desarrollo web de C, por eso se parecen.
	Loop for each
	generalmente vamos a trabajar con arreglos, así que por ello trabajaremos con los Foreach
	los arreglos los declaramos como una variable seguido de la palabra array
	ejemplo: $colors = array("red", "green", "blue", "yellow");
	Los nombres de las clases deben ir con mayúscula (si son dos palabras, ambas deben ir en mayúsculas), o pueden provocar problemas.
	POO en PHP funciona idéntico, existe el encapsulamiento. 
	Los métodos van en minúscula.
	Para acceder a los atributos lo hacemos con $this->
	constructores van con doble guion bajo: __construct(parámetros)
	casi todos los métodos que vamos a estar utilizando van a ser públicas. 
	interfaces y Abstract classes, también llamado contrato. //ni idea, debo investigar mejor.
	En PHP no existe la doble herencia, para eso se crearon los las clases tipo "trait"

	En Laragon podemos tener un servidor local, al prenderlo (usamos apache) podemos accederlo desde un navegador usando 27.01 (creo) o localhost (este sí)
	Al hacer peticiones a alguna instancia que ya no existe o no se encuentra, saldrá el "not found"
	Al entrar al host local deberíamos de ver el primer Index que encuentre en la carpeta raíz de Laragon.
	No olvides hacer un commit/push al finalizar este ejercicio, no me he acostumbrado a hacerlo ni sé hacerlo bien.

-------------------------------------------------------------------------------------------------------------------------------------------------
	10 09 26
	CSS //legué tarde. lol
	Radius para redondear 
	Background?? podemos cambiar el color del radius, imagino o tal.
	perfil con clases de CSS

	termine viendo un tutorial, llegué casi cuando la clase estaba acabando

-------------------------------------------------------------------------------------------------------------------------------------------------

	TP LINK 4590: 77301499
